package ch.frankel.hesge.algo;

import ch.frankel.hesge.algo.model.Order;

import java.util.function.ToDoubleFunction;

public class J_AverageProductPrice implements ToDoubleFunction<Order> {

    /**
     * En utilisant l'API Stream, retourner la moyenne du prix des Product d'un Order.
     */
    @Override
    public double applyAsDouble(Order value) {
        return 0;
    }
}

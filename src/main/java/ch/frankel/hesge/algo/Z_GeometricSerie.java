package ch.frankel.hesge.algo;

import java.util.function.IntFunction;

public class Z_GeometricSerie implements IntFunction<Double> {

    /**
     * En utilisant l'API Stream, calculer la série géométrique 1/1 + 1/2 + 1/4 + 1/8 +1/i + ... + 1/n pour un n donné.
     * En termes mathématiques, il s'agit de ∑ 1/(2^n).
     * Conseil: s'aider de la fonction Math.pow() et de la classe Pair proposée.
     */
    @Override
    public Double apply(int n) {
        return 0.0;
    }
}

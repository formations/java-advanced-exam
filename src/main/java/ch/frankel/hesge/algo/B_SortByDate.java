package ch.frankel.hesge.algo;

import ch.frankel.hesge.algo.model.Person;

import java.util.List;
import java.util.function.Consumer;

public class B_SortByDate implements Consumer<List<Person>> {

    /**
     * Modifier la classe Person pour qu'elle gère l'ordre naturel suivant la date de naissance.
     * Puis implémenter le tri de la liste selon celui-ci.
     */
    @Override
    public void accept(List<Person> persons) {
        // TODO
    }
}

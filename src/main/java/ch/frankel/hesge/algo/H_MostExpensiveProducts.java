package ch.frankel.hesge.algo;

import ch.frankel.hesge.algo.model.Order;
import ch.frankel.hesge.algo.model.Product;

import java.util.function.Function;

public class H_MostExpensiveProducts implements Function<Order, Product> {

    /**
     * En utilisant l'API Stream, retourner le Product le plus cher d'un Order.
     */
    @Override
    public Product apply(Order order) {
        // TODO
        return null;
    }
}

package ch.frankel.hesge.algo;

import ch.frankel.hesge.algo.model.Person;
import ch.frankel.hesge.algo.model.Product;

import java.util.Collection;
import java.util.Map;
import java.util.function.Function;

public class K_MapProductsByPerson implements Function<Collection<Person>, Map<Person, Collection<Product>>> {


    /**
     * En utilisant l'API Stream, reprener l'énoncé E_MapProductsByPerson.
     */
    @Override
    public Map<Person, Collection<Product>> apply(Collection<Person> people) {
        return null;
    }
}
